      integer          ptype(npmax), elem(nvemax,ntmax), edge(2,nemax),
     &                 spts(nspmax), fpts(nfpmax), opts(nopmax),
     &                 bpts(nbpmax), iedge(nemax), edneigh(2,nemax),
     &                 beindx(nemax)
      common/gdata/ptype,elem,edge,spts,fpts,opts,bpts,iedge,edneigh,
     &             beindx
